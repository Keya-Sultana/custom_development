<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="asset_category_type_form_view" model="ir.ui.view">
        <field name="name">asset.category.type.form</field>
        <field name="model">asset.category.type</field>
        <field name="arch" type="xml">
            <form string="Units of Measure categories">
                <group>
                    <field name="name"/>
                </group>
            </form>
        </field>
    </record>
    <record id="asset_category_type_form_action" model="ir.actions.act_window">
        <field name="name">Asset Category Type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">asset.category.type</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
          <p class="oe_view_nocontent_create">
            Click to add a new unit of measure category.
          </p><p>
            Units of measure belonging to the same category can be
            converted between each others. For example, in the category
            <i>'Time'</i>, you will have the following units of measure:
            Hours, Days.
          </p>
        </field>
    </record>
    <menuitem action="asset_category_type_form_action" id="menu_asset_category_type_form_action"
                  parent="account.account_management_menu"
                  sequence="2"/>
</odoo>